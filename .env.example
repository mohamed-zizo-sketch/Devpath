PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=devpath_db
DB_PORT=3306
JWT_SECRET=your_jwt_secret_key
ADMIN_EMAIL=sci.MohamedAbdelaziz01652@alexu.edu.eg

# Public URL of your deployed site (used to build links inside emails)
APP_URL=https://devpath-delta-flame.vercel.app

# Gmail App Password for sending verification/reset emails
# Generate at: Google Account > Security > App Passwords
EMAIL_USER=youraddress@gmail.com
EMAIL_PASS=your16charapppassword

# For real persistent storage instead of the in-memory fallback
# (Neon / Supabase / Vercel Postgres all work — just paste the connection string)
DATABASE_URL=postgres://user:password@host:5432/dbname

# For the AI phase tutor feature
ANTHROPIC_API_KEY=your_anthropic_api_key
